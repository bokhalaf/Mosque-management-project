# سير عمل مدير المسجد (Mosque Manager Workflow) — وحدة التطوع

هذا المستند يوضح التسلسل الكامل لعمليات مدير المسجد ضمن **Volunteer Module**، بدءًا من إنشاء الفرصة التطوعية وحتى إصدار الشهادة، مع كل الـ endpoints المرتبطة بكل مرحلة.

---

## 1. إنشاء فرصة تطوعية (Create Opportunity)

يبدأ مدير المسجد بإنشاء فرصة تطوعية جديدة تحدد الاحتياج (عدد المتطوعين، المدة، الوصف).

**Endpoint:**
```
POST /volunteer/opportunities
```

**Body:**
```json
{
  "mosque_id": 1,
  "title": "تنظيم الصفوف",
  "description": "نحتاج متطوعين لتنظيم صفوف المصلى",
  "required_volunteers": 10,
  "start_date": "2026-07-01",
  "end_date": "2026-07-30"
}
```

---

## 2. (اختياري) تعديل الفرصة

إذا تغيرت التفاصيل (عدد المتطوعين، التواريخ، الوصف) قبل انتهائها، يمكن للمدير تعديلها.

**Endpoint:**
```
PUT /volunteer/opportunities/{id}
```

> ⚠️ ملاحظة: هذه الخطوة اختيارية وتحدث فقط عند الحاجة، وليست جزءًا إلزاميًا من المسار الأساسي.

---

## 3. متابعة الفرص الخاصة بالمدير

يمكن للمدير في أي وقت استعراض كل الفرص التي أنشأها.

**Endpoint:**
```
GET /volunteer/manager/opportunities
```

أو عرض فرصة محددة بالتفصيل:

```
GET /volunteer/opportunities/{id}
```

---

## 4. استعراض طلبات التقديم على الفرصة (Applications)

بعد نشر الفرصة، يبدأ المتطوعون بالتقديم عليها. يستعرض المدير كل الطلبات الواردة.

**Endpoint:**
```
GET /volunteer/opportunities/{opportunityId}/applications
```

---

## 5. قبول أو رفض الطلبات

لكل طلب تقديم، يقرر المدير القبول أو الرفض.

**قبول:**
```
POST /volunteer/opportunities/{applicationId}/approve
```

**رفض:**
```
POST /volunteer/opportunities/{applicationId}/reject
```

---

## 6. إسناد مهمة للمتطوع المقبول

بعد الموافقة على المتطوع، يقوم المدير بإسناد مهمة محددة له ضمن الفرصة.

**Endpoint:**
```
POST /volunteer/tasks
```

**Body:**
```json
{
  "application_id": 5,
  "task_description": "ترتيب صفوف المصلى قبل صلاة الجمعة"
}
```

---

## 7. تسجيل ساعات التطوع وتقييم الأداء

بعد إنجاز المتطوع للمهمة، يقوم المدير بتسجيل عدد الساعات مع تقييم الأداء.

**Endpoint:**
```
POST /volunteer/logs
```

**Body:**
```json
{
  "volunteer_id": 12,
  "opportunity_id": 1,
  "logged_hours": 3.5,
  "manager_evaluation": "ممتاز",
  "notes": "أدى العمل بإتقان"
}
```

> يمكن تكرار هذه الخطوة عدة مرات إذا كان المتطوع يعمل على عدة جلسات ضمن نفس الفرصة.

---

## 8. مراجعة إجمالي الساعات المسجلة

قبل إصدار الشهادة، يتحقق المدير من إجمالي الساعات التي أنجزها المتطوع للتأكد من استحقاقه.

**Endpoint:**
```
GET /volunteer/hours/{volunteerId}/{opportunityId}
```

---

## 9. إغلاق الفرصة التطوعية

بعد اكتمال الفرصة (انتهاء المدة أو اكتمال العدد المطلوب من المتطوعين)، يقوم المدير بإغلاقها.

**Endpoint:**
```
POST /volunteer/opportunities/{id}/close
```

---

## 10. إصدار شهادة للمتطوع

بعد التأكد من الساعات والتقييم، يصدر المدير شهادة تطوع رسمية للمتطوع.

**Endpoint:**
```
POST /volunteer/certificate/{volunteerId}/{opportunityId}
```

**Response:**
```json
{
  "status": true,
  "message": "Certificate issued successfully",
  "data": {
    "id": 1,
    "volunteer_id": 12,
    "opportunity_id": 1,
    "certificate_url": "https://storage.example.com/certs/cert_1.pdf",
    "issued_at": "2026-08-08T18:35:28.896Z"
  }
}
```

---

## 11. تنزيل الشهادة (عند الحاجة)

يمكن للمدير أو المتطوع تنزيل الشهادة كملف PDF في أي وقت بعد الإصدار، دون إعادة توليدها.

**Endpoint:**
```
GET /volunteer/certificate/{volunteerId}/{opportunityId}/download
```

---

## المخطط الكامل للتسلسل

```
إنشاء الفرصة (POST /opportunities)
        │
        ▼
[تعديل الفرصة] (PUT /opportunities/{id})  ← اختياري
        │
        ▼
استعراض الطلبات (GET /opportunities/{id}/applications)
        │
        ▼
قبول / رفض الطلب (POST approve | reject)
        │
        ▼
إسناد مهمة (POST /tasks)
        │
        ▼
تسجيل الساعات والتقييم (POST /logs)   ← يتكرر حسب الحاجة
        │
        ▼
مراجعة إجمالي الساعات (GET /hours/{volunteerId}/{opportunityId})
        │
        ▼
إغلاق الفرصة (POST /opportunities/{id}/close)
        │
        ▼
إصدار الشهادة (POST /certificate/{volunteerId}/{opportunityId})
        │
        ▼
تنزيل الشهادة (GET /certificate/{volunteerId}/{opportunityId}/download)
```

---

## ملاحظات تصميمية (Business Logic)

- **الترتيب المنطقي**: لا يمكن إصدار شهادة (خطوة 10) قبل تسجيل ساعات كافية (خطوة 7)، لذا يُفضّل أن يتحقق الـ Service Layer من هذا الشرط قبل السماح بالإصدار (مثلاً: حد أدنى من الساعات أو أن الفرصة مغلقة).
- **إغلاق الفرصة**: يُفضّل منع إسناد مهام جديدة أو قبول طلبات جديدة بعد إغلاق الفرصة (`close`)، وهذا يحتاج middleware أو check في الـ Service.
- **العلاقة بين Task و Log**: الـ `task_description` نصي بسيط حاليًا، بينما `logs` هي التي تحمل فعليًا الساعات والتقييم؛ قد يُفيد لاحقًا ربط كل log بـ `task_id` محدد بدلاً من `opportunity_id` فقط، لتتبع الأداء على مستوى المهمة الواحدة.
- **Idempotency**: عمليات مثل `approve`/`reject`/`close`/`certificate issue` يجب أن تتحقق من الحالة الحالية (مثلاً: هل الطلب approved مسبقًا؟ هل الشهادة صدرت مسبقًا؟) لمنع التكرار غير المرغوب.
